import Foundation

struct History {
    let smallCount: Int
    let mediumCount: Int
    let largeCount: Int
    
    init(smallCount: Int, mediumCount: Int, largeCount: Int) {
        self.smallCount = smallCount
        self.mediumCount = mediumCount
        self.largeCount = largeCount
    }
}
